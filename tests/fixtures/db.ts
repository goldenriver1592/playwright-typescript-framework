// // this is example of db fixture
// import { DbConnection, createDbConnection } from '../src/utils/db';

// export const dbFixtures = {
//   db: async (_: any, use: any) => {
//     const conn = await createDbConnection(/*…*/);
//     await use(conn);
//     await conn.close();
//   },
// };
